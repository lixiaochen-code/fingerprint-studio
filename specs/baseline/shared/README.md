# Baseline: shared (placeholder)

跨端契约（API 定义、protocol、共享类型）尚未引入。后端引入时本目录启用。

适合放在本目录的内容：

- OpenAPI / GraphQL schema
- protobuf / msgpack 定义
- 跨端共享的 TypeScript types（在物理 monorepo 重构后改为 `packages/shared/`）
- 数据迁移契约

当前阶段：N/A（仅 desktop，无跨端契约）。
